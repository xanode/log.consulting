---
import Icon from '@components/ui/icon.astro';

import HommeClavierReference from '@static/Guillaume_homepage.avif';

import { getCollection } from 'astro:content';
const jobEntries = await getCollection('job');
---

<section class="relative w-full">
    <div class="absolute h-[36rem] bottom-[460px] lg:bottom-0 left-[calc(50vw-160px)] z-0">
        <img class="h-full w-auto" src={HommeClavierReference} width="396" height="672" alt="" />
    </div>
    <div class="flex flex-col lg:flex-row">
        <div class="flex w-full lg:max-w-[50vw] justify-center lg:justify-end items-center bg-black text-white">
            <div class="flex flex-col gap-20 md:items-center lg:items-start lg:py-24 px-6 pt-12 pb-96 lg:pl-[17vw] lg:pr-[10vw] z-0">
                <div class="flex flex-row gap-2">
                    <span class="sr-only">LOG</span>
                    <Icon className="h-16 fill-yellow-log" name="log" />
                    <h1 class="font-extrabold text-4xl lg:text-5xl whitespace-nowrap">GEZ-VOUS !</h1>
                </div>
                <p class="font-bold text-2xl md:text-center lg:text-left">Vous avez envie de rejoindre une équipe dynamique, experte et engagée ?</p>
                <p class="text-yellow-log font-bold text-3xl">Rejoignez la tribu des loggers !</p>
            </div>
        </div>
        <div class="flex w-full bg-gray-log text-black">
            <div class="flex flex-col gap-6 lg:gap-20 mx-auto lg:mx-[7vw] pt-48 pb-8 lg:py-24 z-0">
                <h1 class="rounded-[12px] bg-yellow-log font-bold text-center text-2xl lg:text-3xl px-6 py-4">Les dernières offres à pourvoir !</h1>
                <div class="rounded-[12px] bg-white px-8 py-4 my-4">
                    <ul class="font-bold leading-[3rem] lg:leading-[4rem] text-xl lg:text-2xl list-outside list-[square]">
                        {jobEntries.map(jobEntry => (
                            <li>
                                <a class="decoration-2 lg:decoration-4 hover:underline hover:underline-offset-4" href={`/jobs/${jobEntry.slug}`}>{jobEntry.data.title}</a>
                            </li>
                        ))}
                    </ul>
                </div>
                <a class="bg-black text-white place-self-center lg:place-self-end text-xl lg:text-2xl font-bold rounded-[12px] max-w-sm px-6 py-4" href="/nous-rejoindre">Découvrir toutes nos offres</a>
            </div>
        </div>
    </div>
</section>